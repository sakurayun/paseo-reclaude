import type { ChatSelectionBubbleProps } from "./types";

/** Native stub — the selection bubble is web-only (see the `.web.tsx` file). */
export function ChatSelectionBubble(_props: ChatSelectionBubbleProps): null {
  return null;
}
