# Desktop Agent tab lifecycle

Can Desktop web unmount inactive Agent timelines while retaining lightweight tab, query, draft,
and scroll state, eliminating inactive timeline DOM and reducing renderer memory without regressing
tab-switch consistency or non-Agent panel semantics?
